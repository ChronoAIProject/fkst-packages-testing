kind = "package"
name = "test-artifacts"
persistence_class = "stateless_adapter"

[code]
root = "."

[lib_deps]
libraries = ["contract", "testing_runtime", "workflow", "testkit"]

[conformance]
pack = "conformance/pack.toml"
