schema = 1
runner_protocol = "fkst-declarative-rulepack@1"
owner_package = "module-test-loop"

[[rules]]
id = "module-test-loop-no-fixed-host-config"
severity = "error"
kind = "text_forbid_regex"
include = ["**/*.lua"]
exclude = ["tests/**", "packages/module-test-loop/tests/**"]
pattern = "(localhost:[0-9]+|(APP|HOST)_[A-Z0-9_]+)"
message = "module-test-loop must not encode fixed application endpoints or host environment conventions"
