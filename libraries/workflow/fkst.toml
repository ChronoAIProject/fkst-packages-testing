kind = "library"
name = "workflow"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow_internal"]

[dependency_constraints]
allowed_lib_deps = ["contract", "workflow_internal"]

[library]
name = "workflow"
stable_id = "fkst.library.workflow"
version = "workspace"

[exports]
public = ["workflow.*"]

[visibility]
public = false

[conformance]
pack = "conformance/pack.toml"
