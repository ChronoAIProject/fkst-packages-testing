schema = 1
runner_protocol = "fkst-declarative-rulepack@1"
owner_package = "online-regression"

[[rules]]
id = "online-regression-no-admin-default"
severity = "error"
kind = "text_forbid_regex"
include = ["**/*.lua"]
exclude = ["tests/**"]
pattern = "admin"
message = "online regression defaults must not encode admin-user canaries"
