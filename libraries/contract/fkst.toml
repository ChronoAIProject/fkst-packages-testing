kind = "library"
name = "contract"

[code]
root = "."

[lib_deps]
libraries = []

[dependency_constraints]
allowed_lib_deps = []

[library]
name = "contract"
publishable = true
stable_id = "fkst.library.contract"
version = "workspace"

[exports]
exact = true
public = ["contract.convergence_identity", "contract.environment_factory", "contract.error_facts", "contract.payload", "contract.project_profile", "contract.source_ref", "contract.strings", "contract.testing", "contract.testing_design", "contract.testing_execution", "contract.time", "contract.transition_version", "contract.workflow_qa"]

[visibility]
public = true
