# Issue 794 Walking Skeleton Requirements Blocker

## Status

Blocked as of September 14, 2026.

## Evidence

The approved snapshot for issue 794 does not expose its requirement-bearing
title or body. Both fields contain the typed marker
`[fkst:blocked-github-content:v1 ... why="non-whitelisted-author"]`, and the
available comments contain workflow administration rather than feature
behavior, an external contract, wire grammar, or acceptance criteria.

## Impact

A walking skeleton requires one accepted, observable behavior exercised
through a real architectural path. The available evidence does not identify
such a behavior or path, so an implementation cannot be traced to an
authorized requirement.

No production code, test, placeholder contract, empty scaffold, or substitute
behavior is authorized for issue 794. Treating any of those as the requested
walking skeleton would invent requirements.

## Unblock Condition

Resume specification and implementation only after an authorized source makes
the requirement-bearing title and body, or equivalent explicit feature
behavior and acceptance criteria, available for issue 794.
