kind = "package.composed"
name = "environment-factory"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "testing_runtime"]

[event_deps]
packages = ["browser-readiness"]

[conformance]
pack = "conformance/pack.toml"
function = "conformance.saga_conformance_errors"
