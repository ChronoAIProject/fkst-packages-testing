kind = "package.composed"
name = "module-testing-pipeline"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "testing_ai", "consensus"]

[event_deps]
packages = ["module-test-loop", "testing-runner", "test-artifacts", "test-publication"]

[conformance]
pack = "conformance/pack.toml"
function = "core.saga_conformance_errors"
