kind = "package.composed"
name = "agentic-testing-host"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["workflow", "testkit"]

[event_deps]
packages = ["browser-readiness", "module-testing-pipeline", "module-test-loop", "testing-runner", "test-artifacts", "test-publication"]
