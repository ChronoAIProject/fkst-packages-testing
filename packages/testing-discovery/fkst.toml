kind = "package.composed"
name = "testing-discovery"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "testing_runtime"]

[event_deps]
packages = ["browser-readiness", "platform-test-loop", "testing-pipeline", "module-test-loop", "testing-runner", "test-artifacts", "test-publication"]

[conformance]
pack = "conformance/pack.toml"
function = "core.saga_conformance_errors"
