kind = "package"
name = "test-publication"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit"]

[conformance]
pack = "conformance/pack.toml"
function = "qa_publication.saga_conformance_errors"
